- project:
    check:
      jobs:
        - noop
        - powertrain-build-tox
    gate:
      jobs:
        - noop
        - powertrain-build-tox
        - release-powertrain-build
        - temp-failing-job
