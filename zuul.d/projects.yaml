- project:
    check:
      jobs:
        - noop
        - powertrain-build-tox
    gate:
      jobs:
        - noop
        - powertrain-build-tox
    release:
      jobs:
        - release-powertrain-build
